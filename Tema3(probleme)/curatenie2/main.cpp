#include <bits/stdc++.h>
#include <fstream>
using namespace std;
ifstream in("curatenie.in");
ofstream out("curatenie.out");
int i = 0;
void find_position(vector<int> v,vector<int> rsd)
{
    cout<<v.begin();
}
int main()
{
    unsigned n;
    in>>n;
    vector<int> rsd,srd;
    unsigned st[n],dr[n];
    rsd.reserve(n);
    srd.reserve(n);
    cout<<n;
    for(int i=0;i<n;i++)
        in>>srd[i];
    for(int i=0;i<n;i++)
        in>>rsd[i];
    return 0;
}
